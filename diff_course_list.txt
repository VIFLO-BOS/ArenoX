diff --git a/src/component/_Arenox_dashboard_component/admin_courses_list_component/page.tsx b/src/component/_Arenox_dashboard_component/admin_courses_list_component/page.tsx
index 3604d6a..3ffdb20 100644
--- a/src/component/_Arenox_dashboard_component/admin_courses_list_component/page.tsx
+++ b/src/component/_Arenox_dashboard_component/admin_courses_list_component/page.tsx
@@ -1,14 +1,15 @@
 "use client";
 import { useEffect, useMemo, useState } from "react";
 import toast from "react-hot-toast";
-import { course } from "@/utils/types/course/course";
 import Link from "next/link";
 import { useParams } from "next/navigation";
+import { CourseType } from "@/utils/types/course/course";
+import { EnrollmentType } from "@/utils/types/enrollment/enrollment";
 
 /* @ sort-config-interface : define sorting configuration type */
 
 interface sortConfig {
-  key: keyof course | null;
+  key: keyof CourseType | null;
   dir: "asc" | "desc";
 }
 /* @ admin-courses-component : main admin courses management component with table, modals, and CRUD operations */
@@ -18,8 +19,9 @@ export default function Admin_courses() {
   const dashboard = params.dashboard as string;
 
   /* @ courses-state : manage courses data and table display */
-  const [courses, setcourses] = useState<course[]>([]);
-  const [coursesToTable, setcourseToTable] = useState<course[]>([]);
+  const [courses, setcourses] = useState<CourseType[]>([]);
+  const [coursesToTable, setcourseToTable] = useState<CourseType[]>([]);
+  const [enrollData, setEnrolledData] = useState<EnrollmentType[]>([]);
 
   /* @ fetch-courses : fetch courses data from API on component mount */
 
@@ -27,11 +29,20 @@ export default function Admin_courses() {
     let cancelled = false;
     async function fetchCourse() {
       try {
-        const res = await fetch("/api/data/courses");
-        const result = await res.json();
+        const [coursesRes, userEnrolledRes] = await Promise.all([
+          fetch("/api/data/courses"),
+          fetch("/api/data/enrollments/get-enrollment"),
+        ]);
+        const courseResult = await coursesRes.json();
+        const userEnrr = await userEnrolledRes.json();
+
         if (!cancelled) {
-          setcourses(result.data || []);
-          setcourseToTable(result.data || []);
+          setEnrolledData(userEnrr.data || []);
+        }
+
+        if (!cancelled) {
+          setcourses(courseResult.data || []);
+          setcourseToTable(courseResult.data || []);
         }
       } catch (error) {
         if (!cancelled) {
@@ -50,7 +61,6 @@ export default function Admin_courses() {
   }, []);
 
   /* @ loading-state : manage page loading and error states */
-
   const [loading, setLoading] = useState(true);
   const [error, setError] = useState<string | null>(null);
 
@@ -62,8 +72,6 @@ export default function Admin_courses() {
     return () => clearTimeout(timer);
   }, []);
 
- 
-
   /* @ delete-handler : remove course from table */
 
   const handleDelete = (id: string) => {
@@ -93,8 +101,8 @@ export default function Admin_courses() {
     return (
       item.title.toLowerCase().includes(query.toLocaleLowerCase()) ||
       item.category.toLowerCase().includes(query.toLocaleLowerCase()) ||
-      item.instructor.toLowerCase().includes(query.toLocaleLowerCase()) ||
-      item.level.includes(query.toLocaleLowerCase())
+      item.tutor?.name.toLowerCase().includes(query.toLocaleLowerCase()) ||
+      item.level?.toLowerCase().includes(query.toLocaleLowerCase())
     );
   });
 
@@ -105,7 +113,7 @@ export default function Admin_courses() {
     dir: "asc",
   });
 
-  const handleSort = (key: keyof course) => {
+  const handleSort = (key: keyof CourseType) => {
     let direction: "asc" | "desc" = "asc";
     if (sortConfig.key === key && sortConfig.dir === "asc") {
       direction = "desc";
@@ -121,7 +129,7 @@ export default function Admin_courses() {
     const data = [...filteredData];
 
     data.sort((a, b) => {
-      const key = sortConfig.key as keyof course;
+      const key = sortConfig.key as keyof CourseType;
       const A = (a[key] ?? "").toString().toLowerCase();
       const B = (b[key] ?? "").toString().toLowerCase();
       if (A < B) return sortConfig.dir === "asc" ? -1 : 1;
@@ -133,7 +141,9 @@ export default function Admin_courses() {
 
   /* @ view-course-handler : handle viewing course details */
 
-  const [courseDataToView, setcourseDataToView] = useState<course | null>(null);
+  const [courseDataToView, setcourseDataToView] = useState<CourseType | null>(
+    null,
+  );
   const handleViewCourseForm = (id: string) => {
     const selectCourses = sortedData.find(
       (item) => (item._id || item._id) === id,
@@ -142,6 +152,10 @@ export default function Admin_courses() {
       setcourseDataToView(selectCourses);
     }
   };
+
+
+
+
   return (
     <>
       <div className="bg-white border border-gray-100 rounded-2xl shadow-sm hover:shadow-lg transition-all duration-300 mb-8 overflow-hidden">
@@ -162,9 +176,7 @@ export default function Admin_courses() {
                 className="pl-10 pr-4 py-2 bg-gray-50 border border-gray-200 rounded-xl focus:ring-2 focus:ring-blue-100 focus:border-blue-400 outline-none w-full sm:w-64 transition-all"
               />
             </div>
-            <button
-              className="flex items-center gap-2 px-5 py-2 bg-blue-600 hover:bg-blue-700 text-white font-medium rounded-xl shadow-lg shadow-blue-200 hover:-translate-y-0.5 transition-all duration-300"
-            >
+            <button className="flex items-center gap-2 px-5 py-2 bg-blue-600 hover:bg-blue-700 text-white font-medium rounded-xl shadow-lg shadow-blue-200 hover:-translate-y-0.5 transition-all duration-300">
               <i className="bi bi-plus-lg text-sm"></i> Add Course
             </button>
           </div>
@@ -225,7 +237,7 @@ export default function Admin_courses() {
                 sortedData.map((c, idx) => (
                   <tr
                     key={idx}
-                    className="hover:bg-blue-50/20 transition-colors group"
+                    className="transition-colors group hover:bg-accent-primary/10 dark:hover:bg-accent-primary/20"
                   >
                     <td className="px-6 py-4 text-gray-500 font-medium">
                       #{idx + 1}
@@ -243,7 +255,13 @@ export default function Admin_courses() {
                     <td className="px-6 py-4 text-gray-600 font-medium bg-gray-50/50 rounded-lg m-2 w-fit">
                       {c.category}
                     </td>
-                    <td className="px-6 py-4 text-gray-600">{c.instructor}</td>
+
+
+
+                    <td className="px-6 py-4 text-gray-600">{enrollData.find((e) => e.courseId === c._id)?.userName}</td>
+
+
+
                     <td className="px-6 py-4">
                       <span
                         className={`px-2 py-1 rounded-md text-xs font-semibold ${
@@ -298,7 +316,7 @@ export default function Admin_courses() {
                 ))
               )}
             </tbody>
-            <tfoot className="bg-gray-50/50">
+            <tfoot className="dark:bg-transparent">
               <tr>
                 <td colSpan={9} className="px-6 py-4">
                   <div className="flex flex-col sm:flex-row items-center justify-between gap-4">
