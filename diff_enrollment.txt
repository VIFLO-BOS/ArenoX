diff --git a/src/component/_Arenox_dashboard_component/admin_enrollment_component/page.tsx b/src/component/_Arenox_dashboard_component/admin_enrollment_component/page.tsx
index 8780662..785a04a 100644
--- a/src/component/_Arenox_dashboard_component/admin_enrollment_component/page.tsx
+++ b/src/component/_Arenox_dashboard_component/admin_enrollment_component/page.tsx
@@ -1,113 +1,132 @@
 "use client";
-import React, { useState } from "react";
+import React, { useEffect, useState } from "react";
 import { motion, AnimatePresence } from "framer-motion";
+import toast from "react-hot-toast";
 
-/* @ type-definitions : define types for users, courses, and enrollments */
-
-type User = {
-  id: string;
-  name: string;
-  email: string;
-};
-
-type Course = {
-  id: string;
-  title: string;
-};
-
-type Enrollment = {
-  id: string;
-  userId: string;
-  courseId: string;
-  status: "active" | "completed" | "pending";
-  enrolledAt: string;
-};
-
-/* @ mock-data : sample data for users, courses, and enrollments */
-
-const MOCK_USERS: User[] = [
-  { id: "u1", name: "Alice Johnson", email: "alice@example.com" },
-  { id: "u2", name: "Bob Smith", email: "bob@example.com" },
-  { id: "u3", name: "Charlie Brown", email: "charlie@example.com" },
-];
-
-const MOCK_COURSES: Course[] = [
-  { id: "c1", title: "JavaScript for Beginners" },
-  { id: "c2", title: "React Fundamentals" },
-];
-
-const MOCK_ENROLLMENTS: Enrollment[] = [
-  {
-    id: "e1",
-    userId: "u1",
-    courseId: "c1",
-    status: "active",
-    enrolledAt: "2025-09-01",
-  },
-  {
-    id: "e2",
-    userId: "u2",
-    courseId: "c2",
-    status: "completed",
-    enrolledAt: "2025-07-21",
-  },
-  {
-    id: "e3",
-    userId: "u3",
-    courseId: "c1",
-    status: "pending",
-    enrolledAt: "2025-10-01",
-  },
-];
+export default function Admin_enrollment() {
+  const [enrollments, setEnrollments] = useState<any[]>([]);
+  const [loading, setLoading] = useState(true);
+  const [searchTerm, setSearchTerm] = useState("");
+  const [filterStatus, setFilterStatus] = useState("all");
+  const [sortBy, setSortBy] = useState<"name" | "course" | "status" | "date">(
+    "date",
+  );
 
-/* @ status-config : styling configuration for different enrollment statuses */
+  // Add Enrollment Modal State
+  const [showAddModal, setShowAddModal] = useState(false);
+  const [users, setUsers] = useState<any[]>([]);
+  const [courses, setCourses] = useState<any[]>([]);
+  const [selectedUserId, setSelectedUserId] = useState("");
+  const [selectedCourseId, setSelectedCourseId] = useState("");
+  const [isSubmitting, setIsSubmitting] = useState(false);
 
-const STATUS_CONFIG = {
-  active: {
-    bg: "bg-emerald-100",
-    text: "text-emerald-800",
-    border: "border-emerald-200",
-  },
-  completed: {
-    bg: "bg-blue-100",
-    text: "text-blue-800",
-    border: "border-blue-200",
-  },
-  pending: {
-    bg: "bg-amber-100",
-    text: "text-amber-800",
-    border: "border-amber-200",
-  },
-};
+  // @ enrollment-data : 1. Fetch live data from your new API
+  const fetchEnrollments = async () => {
+    try {
+      const res = await fetch("/api/data/enrollments");
+      const result = await res.json();
+      console.log(result);
+      if (result.status === "success") setEnrollments(result.data);
+    } catch (e) {
+      toast.error("Failed to fetch live enrollments");
+    } finally {
+      setLoading(false);
+    }
+  };
 
-/* @ admin-enrollment-component : enrollment management component with search, filter, and sort */
+  useEffect(() => {
+    fetchEnrollments();
+  }, []);
 
-export default function Admin_enrollment() {
-  /* @ filter-state : manage search, filter, and sort state */
+  // Fetch users and courses when modal opens
+  const openAddModal = async () => {
+    setShowAddModal(true);
+    try {
+      const [usersRes, coursesRes] = await Promise.all([
+        fetch("/api/data/users"),
+        fetch("/api/data/courses"),
+      ]);
+      const usersData = await usersRes.json();
+      const coursesData = await coursesRes.json();
+      if (usersData.status === "success")
+        setUsers(
+          usersData.data.filter((user: any) => user.role === "instructor"),
+        );
+      if (coursesData.status === "success") setCourses(coursesData.data);
+    } catch (e) {
+      toast.error("Failed to load users/courses");
+    }
+  };
 
-  const [searchTerm, setSearchTerm] = useState("");
-  const [filterStatus, setFilterStatus] = useState<
-    "all" | "active" | "completed" | "pending"
-  >("all");
-  const [sortBy, setSortBy] = useState<"name" | "course" | "status" | "date">(
-    "name"
-  );
+  // Handle add enrollment submission
+  const handleAddEnrollment = async () => {
+    if (!selectedUserId || !selectedCourseId) {
+      toast.error("Please select both user and course");
+      return;
+    }
+    setIsSubmitting(true);
+    try {
+      const res = await fetch("/api/data/enrollments", {
+        method: "POST",
+        headers: { "Content-Type": "application/json" },
+        body: JSON.stringify({
+          userId: selectedUserId,
+          courseId: selectedCourseId,
+        }),
+      });
+      const result = await res.json();
+      if (res.ok) {
+        toast.success("Enrollment created successfully!");
+        setShowAddModal(false);
+        setSelectedUserId("");
+        setSelectedCourseId("");
+        fetchEnrollments(); // Refresh the list
+      } else {
+        toast.error(result.error || "Failed to create enrollment");
+      }
+    } catch (e) {
+      toast.error("Failed to create enrollment");
+    } finally {
+      setIsSubmitting(false);
+    }
+  };
 
-  /* @ enrollment-data : combine enrollment data with user and course information */
+  // 2. Action: Unenroll Student
+  const handleUnenroll = async (id: string, name: string) => {
+    if (!confirm(`Unenroll ${name}?`)) return;
+    try {
+      const res = await fetch(`/api/data/enrollments?id=${id}`, {
+        method: "DELETE",
+      });
+      if (res.ok) {
+        toast.success("Unenrolled successfully");
+        setEnrollments((prev) => prev.filter((e) => e._id !== id));
+      }
+    } catch (e) {
+      toast.error("Action failed");
+    }
+  };
 
-  const enrollments = MOCK_ENROLLMENTS.map((en) => {
-    const user = MOCK_USERS.find((u) => u.id === en.userId);
-    const course = MOCK_COURSES.find((c) => c.id === en.courseId);
-    return {
-      ...en,
-      userName: user?.name ?? "Unknown",
-      userEmail: user?.email ?? "N/A",
-      courseTitle: course?.title ?? "N/A",
-    };
-  });
+  // 3. Action: Update Status
+  const handleStatusChange = async (id: string, newStatus: string) => {
+    try {
+      const res = await fetch("/api/data/enrollments", {
+        method: "PATCH",
+        body: JSON.stringify({ id, status: newStatus }),
+        headers: { "Content-Type": "application/json" },
+      });
+      if (res.ok) {
+        toast.success("Status updated");
+        setEnrollments((prev) =>
+          prev.map((e) => (e._id === id ? { ...e, status: newStatus } : e)),
+        );
+      }
+    } catch (e) {
+      toast.error("Update failed");
+    }
+  };
 
   /* @ filtered-sorted-data : apply search, filter, and sort to enrollments */
-
   const filteredEnrollments = enrollments
     .filter((en) => {
       const matchesSearch =
@@ -135,7 +154,7 @@ export default function Admin_enrollment() {
     });
 
   return (
-    <div className="min-h-screen bg-white">
+    <div className="min-h-screen bg-white rounded-lg">
       <motion.div
         initial={{ opacity: 0, y: -20 }}
         animate={{ opacity: 1, y: 0 }}
@@ -159,13 +178,13 @@ export default function Admin_enrollment() {
             <motion.button
               whileHover={{ scale: 1.02 }}
               whileTap={{ scale: 0.98 }}
+              onClick={openAddModal}
               className="px-4 py-2 bg-indigo-600 text-white text-sm font-medium rounded-lg shadow-sm hover:bg-indigo-700 transition-colors"
             >
               + Add Enrollment
             </motion.button>
           </div>
         </div>
-
         {/* Controls */}
         <div className="bg-white mb-6">
           <div className="px-2">
@@ -181,7 +200,6 @@ export default function Admin_enrollment() {
                 />
                 <i className="bi bi-search absolute left-3.5 top-1/2 -translate-y-1/2 text-gray-400"></i>
               </div>
-
               {/* Filters */}
               <div className="flex flex-wrap gap-2">
                 {(["all", "active", "completed", "pending"] as const).map(
@@ -199,17 +217,16 @@ export default function Admin_enrollment() {
                     >
                       {status.charAt(0).toUpperCase() + status.slice(1)}
                     </motion.button>
-                  )
+                  ),
                 )}
               </div>
-
               {/* Sort */}
               <div className="relative">
                 <select
                   value={sortBy}
                   onChange={(e: React.ChangeEvent<HTMLSelectElement>) =>
                     setSortBy(
-                      e.target.value as "name" | "course" | "status" | "date"
+                      e.target.value as "name" | "course" | "status" | "date",
                     )
                   }
                   className="px-4 py-2.5 bg-gray-50 border border-gray-200 rounded-xl text-sm focus:bg-white focus:ring-2 focus:ring-blue-100 focus:border-blue-500 outline-none appearance-none pr-8 cursor-pointer"
@@ -224,7 +241,6 @@ export default function Admin_enrollment() {
             </div>
           </div>
         </div>
-
         {/* Modern Table */}
         <div className="bg-white border border-gray-100 rounded-2xl shadow-sm overflow-hidden hover:shadow-lg transition-shadow duration-300">
           <div className="overflow-x-auto w-full">
@@ -267,7 +283,7 @@ export default function Admin_enrollment() {
                   ) : (
                     filteredEnrollments.map((en, index) => (
                       <motion.tr
-                        key={en.id}
+                        key={en._id}
                         initial={{ opacity: 0, x: -20 }}
                         animate={{ opacity: 1, x: 0 }}
                         transition={{ delay: index * 0.05 }}
@@ -284,7 +300,7 @@ export default function Admin_enrollment() {
                               <span className="text-indigo-600 font-semibold text-sm">
                                 {en.userName
                                   .split(" ")
-                                  .map((n) => n[0])
+                                  .map((n: string) => n[0])
                                   .join("")}
                               </span>
                             </div>
@@ -304,11 +320,16 @@ export default function Admin_enrollment() {
                           </div>
                         </td>
                         <td className="px-6 py-2 whitespace-nowrap">
+                          {/* Γ£à FIX 2: Using inline Tailwind classes instead of STATUS_CONFIG */}
                           <span
-                            className={`inline-flex px-2.5 py-1 rounded-full text-xs font-medium ${
-                              STATUS_CONFIG[en.status].bg
-                            } ${STATUS_CONFIG[en.status].text} ${
-                              STATUS_CONFIG[en.status].border
+                            className={`inline-flex px-2.5 py-1 rounded-full text-xs font-medium border ${
+                              en.status === "active"
+                                ? "bg-green-50 text-green-700 border-green-200"
+                                : en.status === "completed"
+                                  ? "bg-blue-50 text-blue-700 border-blue-200"
+                                  : en.status === "pending"
+                                    ? "bg-yellow-50 text-yellow-700 border-yellow-200"
+                                    : "bg-red-50 text-red-700 border-red-200"
                             }`}
                           >
                             {en.status.charAt(0).toUpperCase() +
@@ -330,6 +351,9 @@ export default function Admin_enrollment() {
                             <motion.button
                               whileHover={{ scale: 1.05 }}
                               whileTap={{ scale: 0.95 }}
+                              onClick={() =>
+                                handleUnenroll(en._id, en.userName)
+                              }
                               className="px-3 py-1.5 text-xs font-medium text-red-600 bg-red-50 rounded-lg hover:bg-red-100 transition-colors"
                             >
                               Unenroll
@@ -345,6 +369,90 @@ export default function Admin_enrollment() {
           </div>
         </div>
       </motion.div>
+
+      {/* Add Enrollment Modal */}
+      <AnimatePresence>
+        {showAddModal && (
+          <motion.div
+            initial={{ opacity: 0 }}
+            animate={{ opacity: 1 }}
+            exit={{ opacity: 0 }}
+            className="fixed inset-0 bg-black/50 flex items-center justify-center z-50"
+            onClick={() => setShowAddModal(false)}
+          >
+            <motion.div
+              initial={{ scale: 0.9, opacity: 0 }}
+              animate={{ scale: 1, opacity: 1 }}
+              exit={{ scale: 0.9, opacity: 0 }}
+              onClick={(e) => e.stopPropagation()}
+              className="bg-white rounded-2xl shadow-xl p-6 w-full max-w-md mx-4"
+            >
+              <h2 className="text-xl font-bold text-gray-900 mb-4">
+                Add New Enrollment
+              </h2>
+
+              {/* User Select */}
+              <div className="mb-4">
+                <label className="block text-sm font-medium text-gray-700 mb-2">
+                  Select Instructor
+                </label>
+                <select
+                  value={selectedUserId}
+                  onChange={(e) => setSelectedUserId(e.target.value)}
+                  className="w-full px-4 py-2.5 bg-gray-50 border border-gray-200 rounded-xl focus:bg-white focus:ring-2 focus:ring-indigo-100 focus:border-indigo-500 outline-none"
+                >
+                  <option value="">-- Select a Instructor --</option>
+                  {users.map((user) => (
+                    <option key={user._id} value={user._id}>
+                      {user.name} ({user.email})
+                    </option>
+                  ))}
+                </select>
+              </div>
+
+              {/* Course Select */}
+              <div className="mb-6">
+                <label className="block text-sm font-medium text-gray-700 mb-2">
+                  Select Course
+                </label>
+                <select
+                  value={selectedCourseId}
+                  onChange={(e) => setSelectedCourseId(e.target.value)}
+                  className="w-full px-4 py-2.5 bg-gray-50 border border-gray-200 rounded-xl focus:bg-white focus:ring-2 focus:ring-indigo-100 focus:border-indigo-500 outline-none"
+                >
+                  <option value="">-- Select a course --</option>
+                  {courses.map((course) => (
+                    <option key={course._id} value={course._id}>
+                      {course.title}
+                    </option>
+                  ))}
+                </select>
+              </div>
+
+              {/* Buttons */}
+              <div className="flex gap-3">
+                <motion.button
+                  whileHover={{ scale: 1.02 }}
+                  whileTap={{ scale: 0.98 }}
+                  onClick={() => setShowAddModal(false)}
+                  className="flex-1 px-4 py-2.5 border border-gray-200 text-gray-700 rounded-xl hover:bg-gray-50 transition-colors"
+                >
+                  Cancel
+                </motion.button>
+                <motion.button
+                  whileHover={{ scale: 1.02 }}
+                  whileTap={{ scale: 0.98 }}
+                  onClick={handleAddEnrollment}
+                  disabled={isSubmitting}
+                  className="flex-1 px-4 py-2.5 bg-indigo-600 text-white rounded-xl hover:bg-indigo-700 transition-colors disabled:opacity-50"
+                >
+                  {isSubmitting ? "Adding..." : "Add Enrollment"}
+                </motion.button>
+              </div>
+            </motion.div>
+          </motion.div>
+        )}
+      </AnimatePresence>
     </div>
   );
 }
